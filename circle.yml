machine:
  python:
    version: 3.6.2

checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  override:
    - pip install awscli

test:
  override:
    - echo "test"
